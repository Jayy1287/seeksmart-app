# Feature Layer

This folder contains UI and product modules grouped by domain.

Examples:

- `tools`
- `categories`
- `submissions`
- `admin`

Feature components should call server/query helpers through pages or route handlers, not directly hide database logic inside presentational components.

